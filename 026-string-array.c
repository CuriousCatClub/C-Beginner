#include <stdio.h>

int main(void)
{
  // Assigning string to an array
  // It is mutable - you can do s[6] = 'D';
  char s[]  = "Do or do not. There is no try. - Yoda";

  printf("%s\n", s);

  return 0;
}
