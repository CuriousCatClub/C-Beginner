/* 
  Hello world Program
  compile with: gcc -o name-of-exec name-of-src-code
*/

#include <stdio.h>
int main(void)
{
  printf("Hello, world!\n");  // will print 'Hello, world!' on the screen

  return 0;                   // return 0 = all good to the OS
}
